{
  "name": "mps-simulateur-avatars",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "demo": "vite build && node scripts/package-demo.mjs",
    "preview": "wrangler dev",
    "test": "node --test tests/*.test.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": { "three": "0.180.0" },
  "devDependencies": { "vite": "7.3.6", "wrangler": "4.131.1" },
  "packageManager": "pnpm@11.19.0",
  "engines": { "node": ">=22" }
}
